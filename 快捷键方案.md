# 快捷键方案

设置入口:`窗口 > 系统偏好`(Mac:`SketchUp > 偏好设置`)> 快捷键,顶部搜索框输工具名过滤。同一命令可绑多个键,改完立即生效。

## 推荐键位

### 绘图

| 键 | 工具 | 备注 |
|---|---|---|
| 空格 | 选择 | 默认 |
| L | 直线 | 默认 |
| A | 圆弧 | 默认 |
| C | 圆形 | 默认 |
| R | 矩形 | 默认 |
| B | 颜料桶 | 默认 |

### 编辑

| 键 | 工具 | 备注 |
|---|---|---|
| M | 移动 | 默认;按住 Ctrl = 复制 |
| Q | 旋转 | 默认 |
| S | 缩放 | 默认;数值框敲 -1 = 镜像 |
| P | 推拉 | 默认 |
| F | 偏移 | **建议设置**(默认无) |
| E | 擦除 | 默认 |
| T | 卷尺 | 默认 |
| X | 量角器 | **建议设置** |

### 视图

| 键 | 工具 | 备注 |
|---|---|---|
| O | 环绕观察 | 默认 |
| H | 平移 | 默认 |
| Z | 缩放 | 默认 |
| Shift+Z | 全屏缩放(显示全部) | 默认 |
| F2 | 顶视图 | **建议设置** |
| F3 | 前视图 | **建议设置** |
| F4 | 左视图 | **建议设置** |
| F5 | 右视图 | **建议设置** |
| F6 | 后视图 | **建议设置** |
| F7 | 等轴测 | **建议设置** |

### 组织

| 键 | 工具 | 备注 |
|---|---|---|
| G | 制作群组 | **建议设置** |
| Shift+G | 制作组件 | **建议设置** |

## 迁移到新电脑

SketchUp 2021 及以上版本支持在欢迎窗口导出/导入首选项(含快捷键);没有该功能的旧版本,照上表手动设一遍,10 分钟一次到位。

## 记不住怎么办

先只设最痛的 5 个:**F 偏移、G 群组、X 量角器、F2 顶视图、F7 等轴测**,用顺了再补齐。
