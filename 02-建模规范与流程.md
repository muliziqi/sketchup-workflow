# 02 · 建模规范与流程

## 三条铁律

1. **先成组,再编辑** —— 任何推拉/移动之前,这堆图形必须已经是群组或组件,否则所有面会粘连在一起。
2. **先体量,后细部** —— 先把"盒子"摆对,再做线脚;细部永远最后做。
3. **精度服务于出图** —— 出什么图画什么细节;远景的东西不建细节。

## 标准流程(7 步)

### ① 导入底图

`文件 > 导入` 选 dwg/dxf,**在导入对话框"选项"里核对单位**(CAD 图是毫米还是米)。导入后:

- 全选 → 右键"制作组件",命名 `REF-底图`,归入标记 `00-底图`
- 右键 > 锁定
- 用卷尺/辅助线复核轴网、层高

### ② 搭控制线

轴网、总图控制线画好后归入 `10-参考`,出图前可整体隐藏。

### ③ 体量(大关系)

- 画闭合平面轮廓 → 推拉(P)到标高;尺寸直接用键盘输入,不要拖到"大概"
- 每个标准层单独成组,复制上楼
- 结构(柱/梁/剪力墙)归入 `01-结构`

### ④ 构件

| 构件 | 做法 | 注意 |
|---|---|---|
| 墙体 | 矩形+偏移(F)+推拉 | 单色模式检查面方向 |
| 门窗 | 做成"带切割体"的组件 | 见下方"组件开洞" |
| 楼板 | 沿墙内边描面推拉 | 板厚单独一段,别和墙粘连 |
| 屋顶 | 原生推拉或 1001bit/放样插件 | 坡屋顶先画剖面料线 |
| 楼梯 | 1001bit 或插件 | 控制面数,看不见的背面不建 |

**组件开洞**:门/窗组件内做一个与墙等厚、贯通墙体的封闭矩形作为"切割体",插入墙体后自动开洞(未生效时右键该组件 > 剖切开口)。这样改墙长、移动门窗,洞口自动跟随。

### ⑤ 细部

线脚、栏杆、门套……只在近景/出图范围做。能用插件(RoundCorner、Curviloft、Profile Builder)的不手抠。

### ⑥ 家具与配景

从组件库/3D Warehouse 调用 → 先按 `03` 的"组件库卫生守则"清理,再归入 `05-家具` / `06-配景`。

### ⑦ 核对

跑 `扩展程序 > SU工作流 > 03 模型体检报告`,看总面数和最"重"的组件;对照文末清单。

## 操作规范速查

| 情形 | 规范做法 |
|---|---|
| 画完闭合面准备推拉 | 先右键"制作组件/群组" |
| 发现蓝灰色反面 | `视图 > 面样式 > 单色` 检查,右键 > 反转面 |
| 布尔运算 | `工具 > 实体工具`,两个对象必须都是"实体"(单壳无破洞) |
| 临时参考线 | 用完删除,或统一归 10-参考 |
| 跨楼层复制 | Ctrl+移动复制,输入层高;多层用 xN 阵列 |
| 要改被缩放过的组件 | 右键 > 重置缩放,再改尺寸 |
| 删不掉的面/线 | 先查是否被锁定或藏在群组内 |

## 命名规范

| 对象 | 格式 | 示例 |
|---|---|---|
| 组件 | 类别-名称-规格 | 墙-200-首层 / 门-M1021 / 树-香樟-8m |
| 场景 | SC-编号-名称 | SC-01-南立面 |
| 材质 | MAT_部位_名称 | MAT_墙面_白乳胶漆 |

## 数值输入技巧(工具激活时直接敲键盘)

- 画线:敲 `3000` 定长度;敲 `3000,4000` 定相对坐标点
- 推拉:敲 `4500` 定高度
- 复制:移动(M)+Ctrl,敲距离;再敲 `x4` 得 4 份,`/5` 五等分
- 旋转(Q)+Ctrl 复制旋转;缩放(S)敲 `-1` 镜像

## 常见坑

1. 没成组就推拉 → 全模型面粘连,后期拆不动。
2. CAD 底图没核对单位 → 模型差 1000 倍或 25.4 倍。
3. 用缩放改组件尺寸 → 定义被污染,所有实例变形;应"重置缩放"后重画。
4. 面数爆炸 → 大头通常是树和配景,见 03 的减面守则。
5. 直接在未锁定的底图上建模 → 一动全乱。

## 建模自查清单

- [ ] 所有体量都在群组/组件里,最外层只有一个总组
- [ ] 单色模式下没有大片蓝灰反面
- [ ] 组件都有名字
- [ ] 门窗是"切割组件",不是在墙上手抠的洞
- [ ] 07-临时、10-参考 可整体隐藏
- [ ] 跑过体检报告,面数在预算内
