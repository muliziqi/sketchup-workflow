# 03 · 组件、群组与标记的组织

## 群组 vs 组件 怎么选

| | 群组 Group | 组件 Component |
|---|---|---|
| 本质 | 隔离几何 | 隔离 + 关联定义 |
| 改一个,其他跟着变 | ✗ | ✓(同一定义的实例) |
| 相同内容占用内存 | 各存一份 | 只存一份定义 |
| 可以切墙开洞 | ✗ | ✓(切割组件) |
| 可存入组件库、跨文件复用 | ✗ | ✓ |
| 适用 | 一次性、独立的体量 | 门窗、家具、柱网、树、灯等重复/复用件 |

口诀:**会出现第二次的东西,一律组件**。

## 层级结构(建议不超过 4 层)

```
模型总组(壳)                      ← 最外层永远套一个总组
├─ 区块/楼栋
│  ├─ 标准层
│  │  ├─ 墙体(群组)
│  │  ├─ 门窗(组件)
│  │  └─ 楼梯(组件)
```

- 内部几何不裸露,双击进入编辑,编辑完点外部退出。
- 嵌套超过 4~5 层,通常意味着该按楼栋/专业拆文件了(见 08)。

## 标记(Tag)的分工

标记**不**用来组织模型,只负责三件事:

1. 显隐开关(汇报时关掉家具/配景)
2. 出图过滤(平面图隐藏 06-配景)
3. 渲染时区分对象

归属关系看组件层级,显示控制看标记——两套系统各干各的,别混用。

> 2020 以前版本里标记叫"图层(Layers)",菜单和 API 仍沿用 layers 这个词。

## 组件库卫生守则(3D Warehouse / 外来模型)

1. **下载前**:看三角面数——家具 ≤2 万、树 ≤5 万;太重的换一个。
2. **下载后(必做)**:按命名规范重命名、归到对应标记、炸开检查有无残留 CAD 线、跑 02 脚本清理多余材质。
3. **重的常用件**:用 Skimp/Transmutr 减面后再入库。
4. **入库**:做成组件 → 另存为 .skp,放进 `02-SU源文件/组件库/` 的分类文件夹。
5. 优先复用自己的库;Warehouse 是补充,不是主力。

## 常用组件操作

| 目的 | 操作 |
|---|---|
| 编辑一个实例,全部同定义实例更新 | 直接双击编辑即可 |
| 让某个实例不再跟随定义 | 右键 > 设为唯一 |
| 组件被缩放过、尺寸乱 | 右键 > 重置缩放 |
| 跨文件搬运并保持原位 | 复制 → 目标文件 `编辑 > 就地粘贴` |
| 更新从外部 .skp 导入的组件 | 右键 > 重新加载 |

## 自查清单

- [ ] 最外层只有一个总组
- [ ] 重复出现的元素都是同一组件定义(不是复制粘贴的散面)
- [ ] 标记只有标准的 9 个,没有"图层 1"这类垃圾
- [ ] Warehouse 下载件都重命名+清理过
- [ ] 没有超过 4~5 层的嵌套
