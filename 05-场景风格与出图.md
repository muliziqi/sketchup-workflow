# 05 · 场景、风格与出图

## 场景(Scene)是什么

场景 = 一组"快照开关":记住**相机、样式、标记显隐、阴影、剖切面**的组合。建好场景后,一键跳转、批量出图、LayOut 联动全靠它。

**关键动作**:场景面板"属性"里勾选要保存的项——相机、样式、标记位置、阴影设置、剖切面,少勾哪个,哪个切换场景就不生效。

## 标准场景集(模板已建好)

| 场景 | 视图 | 投影 |
|---|---|---|
| SC-00-顶视图 | 相机 > 标准视图 > 顶部 | 平行 |
| SC-01~04 四个立面 | 前/后/左/右 | 平行 |
| SC-05-轴测 | 等轴测 | 平行 |
| SC-06-透视 | 自由机位 | 透视 + 两点透视 |

透视原则:**方案透视一律开两点透视**(`相机 > 两点透视`),避免歪楼。

## 风格配方(`窗口 > 样式`)

| 用途 | 面样式 | 边线 | 备注 |
|---|---|---|---|
| 检查模型 | 单色 | 无 | 看反面/破面 |
| 日常白模 | 单色 | 轮廓线 | 建模主力 |
| 汇报成图 | 着色+贴图 | 轮廓线 | 出图前 |
| 线稿/草图 | 消隐 | 轮廓线+端点线 | 搭配 LayOut |

- 边线只开"轮廓线";端点线、抖动线出图慢且显乱,慎用。
- 风格跟着场景走:改样式后记得"更新"对应场景。

## 阴影

- `窗口 > 阴影`,时间/日期配合 `模型信息 > 地理位置` 使用。
- 日照分析常用:大寒/冬至的 9:00 / 12:00 / 15:00 对比。
- 日常出图默认 6 月下旬 14:00 左右,再按朝向微调。
- **建模时关阴影,出图再开**,性能差很多。

## 剖面

1. `工具 > 剖切面`,点击要剖的墙体/楼板,移动到剖切位置
2. 选中剖切面可反转剪切方向
3. 把剖切面状态存进场景(场景属性勾了"剖切面"才生效)
4. 剖切填充:`窗口 > 模型信息 > 剖切面` 设填充颜色;出正式剖面图走 LayOut

## 导出

| 需求 | 路径 | 备注 |
|---|---|---|
| 单张图 | `文件 > 导出 > 二维图形` | 选 PNG,"选项"里设分辨率(如 3840×2160) |
| 全场景批量 | `扩展程序 > SU工作流 > 04 批量导出场景图片` | 按 SC 编号输出到模型旁 exports/ |
| 动画 | `文件 > 导出 > 动画` | 场景顺序即播放顺序 |
| 给渲染器/其他软件 | `文件 > 导出 > 三维模型` | DAE 通用;FBX、OBJ 给渲染器 |
| 图纸排版 | `文件 > 发送到 LayOut` | 见下 |

## LayOut 出图纸(推荐流程)

1. `文件 > 发送到 LayOut`,选纸张模板
2. 从右侧"场景"面板把场景拖进图框,设比例、样式、剖切
3. 加尺寸、标注、图例(标注随视口比例自动换算)
4. 模型改动后:回 SketchUp 保存 → LayOut 里右键视口 > 更新模型引用
5. `文件 > 导出 > PDF`

## 出图前清单

- [ ] 跑过"02 一键清理模型"并另存
- [ ] 每个场景属性勾全,逐个切换检查
- [ ] 立面=平行投影,透视=两点透视
- [ ] 阴影设置已存进场景
- [ ] 剖切位置存进场景,剖切填充色已设置
- [ ] 导出分辨率满足用途(PPT 1080P;打印规格见 06)
