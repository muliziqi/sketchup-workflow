# 08 · 文件管理与协作

## 项目文件夹模板(新项目复制一份)

```
项目根目录/
├── 01-参考/
│   ├── CAD/          原始 dwg(只读,别在上面干活)
│   ├── 图片/         实拍、意向图
│   └── 资料/         任务书、规范、会议纪要
├── 02-SU源文件/
│   ├── 项目简称_方案_v01.skp
│   ├── 项目简称_室内_v01.skp
│   ├── exports/      (脚本自动生成:场景导出图)
│   └── 组件库/       自建组件 .skp,按类别分子文件夹
├── 03-assets/
│   └── textures/     贴图原图
├── 05-渲染/          渲染工程与成图
├── 06-LayOut/        图纸
└── 99-归档/          废弃方案、旧版本
```

## 版本规则

- 文件名:`项目简称_内容_v两位数.skp`,如 `某某项目_方案_v03.skp`
- **每个工作日开始时另存一个新版本**;大改(炸开重组/换方案)前手动另存
- 给甲方看的里程碑版,同时拷一份进 `99-归档`
- 开启 `系统偏好 > 常规 > 创建备份`,至少有 .skb 兜底

## 交文件给同事/渲染/打印前

1. 跑 `扩展程序 > SU工作流 > 02 一键清理模型`
2. 删除或锁定 07-临时、10-参考 的内容
3. 另存时可选低版本格式,给没升级的同事
4. 对方打不开/丢材质 → 八成是版本或贴图问题,按上面检查

## 多人协作

- **Trimble Connect**(官方云):SketchUp 2021+ 界面内置 Connect 入口,上传、下载、版本管理,小团队够用。
- **分工拆分**:大项目按楼栋/专业拆成多个 .skp,每人负责一个;汇总文件用 `文件 > 导入` 把分项作为**组件**插入,分项更新后右键该组件 > 重新加载即可同步。
- 不要两个人同时编辑同一个文件——SketchUp 没有合并功能。

## 与其他软件互通

| 方向 | 格式 | 说明 |
|---|---|---|
| CAD → SU | DWG / DXF | 导入时核对单位,见 02 |
| SU → CAD | DWG / DXF | 按场景导出二维图(平面/立面),也可导三维 |
| SU ↔ Revit | IFC(Pro) | 以几何为主,参数有限,适合管模/翻模 |
| SU → 渲染 | FBX / OBJ / DAE | 见 06 |
| SU → 3D 打印 | STL | 先用实体工具确认"实体"再导出 |

## 归档

项目结束:整个根目录打包 zip,确认包含 skp 源文件 + assets(贴图源)+ exports 成果;`99-归档` 里的过程版本可酌情删减。
