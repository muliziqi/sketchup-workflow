# 01 · 准备与模板

> 目标:花 20 分钟做出一个"随开随用"的模板,以后每个新项目都从它开始。做一次,用三年。

## 为什么先做模板

SketchUp 默认模板单位、精度不合适;每个项目手动改单位、建标记、建场景既浪费时间又容易忘。模板把这些一次性固化:**单位、标记体系、标准场景、样式、地理位置**。

## 第 1 步 · 单位与精度

`窗口 > 模型信息 > 单位`

| 项 | 建议 |
|---|---|
| 格式 | 十进制 |
| 单位 | 毫米(室内/构件) 或 米(建筑方案) |
| 精度 | 0(方案阶段) / 2(施工深化) |
| 启用长度捕捉 | 关(需要时再开) |

> 精度只影响显示,不影响内部计算,不必纠结。

## 第 2 步 · 标记体系(2020 以前叫"图层")

在默认托盘的"标记"面板里建 9 个标准标记,数字前缀保证排序稳定:

| 标记 | 放什么 |
|---|---|
| 00-底图 | CAD 底图、参考图(通常锁定) |
| 01-结构 | 柱、梁、剪力墙 |
| 02-墙体门窗 | 墙、门、窗组件 |
| 03-楼板屋面 | 楼板、坡道、屋顶 |
| 04-楼梯 | 楼梯、扶手 |
| 05-家具 | 家具组件 |
| 06-配景 | 树、人、车 |
| 07-临时 | 草稿、对比方案(交付前删) |
| 10-参考 | 辅助线、控制线 |

原则:**组织模型靠组件层级,标记只负责显隐**。详见 `03-组件标记与组织`。

## 第 3 步 · 样式

`窗口 > 样式`,建两个常用样式并收藏:

- **S-白模**:面样式=单色,开轮廓线,关天空地面 → 检查模型、做分析图
- **S-汇报**:面样式=着色并显示贴图,开轮廓线,地面开 → 日常展示

## 第 4 步 · 场景集

按 `SC-编号-名称` 建 7 个标准场景(脚本"01 初始化标准模板"会自动建):

| 场景 | 要求 |
|---|---|
| SC-00-顶视图 | 相机 > 标准视图 > 顶部,平行投影 |
| SC-01~04 四个立面 | 前/后/左/右,平行投影 |
| SC-05-轴测 | 等轴测,平行投影 |
| SC-06-透视 | 透视投影 + 两点透视 |

建场景后,在场景面板"属性"里勾选要保存的内容:**相机、样式、标记位置、阴影设置、剖切面**全部勾上。

## 第 5 步 · 地理位置(做阴影/日照用)

`窗口 > 模型信息 > 地理位置` 填项目城市;或 `文件 > 地理位置 > 添加位置` 从地图选取。

## 第 6 步 · 快捷键

见 [快捷键方案.md](快捷键方案.md),10 分钟设完最常用的十几个。

## 第 7 步 · 另存为模板

`文件 > 另存为模板`,命名如"标准-建筑-米"。以后 `文件 > 新建` 时选它作为起点。

## 系统偏好建议(`窗口 > 系统偏好`)

| 项 | 建议 |
|---|---|
| 常规 > 自动保存 | 开,间隔 5~10 分钟 |
| 常规 > 创建备份 | 开(生成 .skb 兜底文件) |
| OpenGL | 勾"使用硬件加速";大模型时可降低抗锯齿 |
| 扩展 | 只勾自己用的插件,清单见 `07-插件清单` |

## 常见坑

- 单位是英寸却按毫米输入 → 模型小 25.4 倍;动手前先看右下角度量单位。
- 模板里塞了大量组件和材质 → 每个新文件都变大;模板保持空场景。
- 把材质库当模板内容 → 材质库是全局的,不随文件走,不用放进模板。

## 完成清单

- [ ] 单位、精度按上表设好
- [ ] 9 个标准标记建好
- [ ] 两个样式(S-白模 / S-汇报)建好并收藏
- [ ] 7 个标准场景建好且属性勾全
- [ ] 地理位置、快捷键设好
- [ ] 已另存为模板,新建文件试了一次
